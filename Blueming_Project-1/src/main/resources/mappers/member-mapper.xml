<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">

<resultMap id="memberResultSet" type="member">
		<id column="MEMBER_ID" property="memberId" />
		<result column="LOGIN_ID" property="loginId" />
		<result column="LOGIN_PWD" property="loginPwd" />
		<result column="NAME" property="name" />
		<result column="PHONE" property="phone" />
		<result column="EMAIL" property="email" />
		<result column="ADDRESS" property="address" />
		<result column="HIRE_DATE" property="hireDate" />
		<result column="RETIRE_DATE" property="retireDate" />
		<result column="DEPARTMENT_ID" property="departmentId" />
		<result column="POSITION_ID" property="positionId" />
		<result column="ROLE" property="role" />
		<result column="LEAVE_START_DATE" property="leaveStartDate" />
		<result column="LEAVE_END_DATE" property="leaveEndDate" />
	</resultMap>
	
	<select id="loginMember" parameterType="member" 
							 resultMap="memberResultSet">
		SELECT *
		  FROM MEMBER
		 WHERE LOGIN_ID = #{loginId}
		   AND STATUS = 'Y'
		 
	
	</select>
	
		<update id="updatePwd" parameterType="member">
		UPDATE MEMBER
		   SET LOGIN_PWD = #{loginPwd}
		 WHERE LOGIN_ID = #{loginId}
		   AND STATUS = 'Y'
		</update>
		
		<update id="updateMember" parameterType="member">
		UPDATE MEMBER
		   SET NAME = #{name}
		     , PHONE = #{phone}
		     , EMAIL = #{email}
		     , ADDRESS = #{address}
		 WHERE LOGIN_ID = #{loginId}
		   AND STATUS = 'Y'
		</update>
		
			<!-- 회원 탈퇴용 쿼리문 -->
	<update id="deleteMember" parameterType="string">
		UPDATE MEMBER
		   SET STATUS = 'N'
		 WHERE USER_ID = #{loginId}
		   AND STATUS = 'Y'
	</update>
	
	<!-- 아이디 중복 체크용 쿼리문 -->
	<select id="idCheck" parameterType="string" resultType="_int">
		SELECT COUNT(*)
		  FROM MEMBER
		 WHERE USER_ID = #{checkId}
	</select>
	
	<select id="findIdByEmail" parameterType="com.kh.blueming.member.model.vo.Member" resultType="string">
        SELECT LOGIN_ID
        FROM MEMBER
        WHERE NAME = #{name}
          AND EMAIL = #{email}
          AND STATUS = 'Y' </select>

    <select id="checkMemberExist" parameterType="com.kh.blueming.member.model.vo.Member" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE LOGIN_ID = #{loginId}
          AND NAME = #{name} 
          AND EMAIL = #{email}
          AND STATUS = 'Y'
    </select>

    <update id="resetPassword" parameterType="com.kh.blueming.member.model.vo.Member">
        UPDATE MEMBER
        SET LOGIN_PWD = #{loginPwd}
        WHERE LOGIN_ID = #{loginId}
          AND EMAIL = #{email}
    </update>
  
 

</mapper>